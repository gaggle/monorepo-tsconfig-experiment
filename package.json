{
  "name": "monorepo-tsconfig-experiment",
  "scripts": {
    "build": "tsc -b",
    "install-all": "npm i && cd apps/app && npm i && cd ../../libs/lib; npm i"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^4.3.5"
  }
}
